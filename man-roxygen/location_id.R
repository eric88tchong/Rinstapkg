#' @param location_id integer; the unique id to identify a place which can be 
#' found in the \code{"pk"}, \code{"external_id"}, or \code{"facebook_places_id"} fields 
#' on other objects returned via the Instagram API
